services:
  episodely:
    build: .
    ports:
      - "4285:4285"
    environment:
      PORT: 4285
      SESSION_SECRET: change-me
      DB_PATH: /data/episodely.sqlite
    volumes:
      - ./data:/data
