services:
  episodely:
    image: ghcr.io/keenfann/episodely:latest
    restart: unless-stopped
    ports:
      - "4285:4285"
    environment:
      PORT: 4285
      DB_PATH: /data/episodely.sqlite
    volumes:
      - ./data:/data
