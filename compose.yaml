services:
  episodely:
    build: .
    ports:
      - "3000:3000"
    environment:
      PORT: 3000
      SESSION_SECRET: change-me
      DB_PATH: /data/episodely.sqlite
    volumes:
      - ./data:/data
